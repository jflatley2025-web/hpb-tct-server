# ───────────────────────────────────────────────
# HPM–TCT v19 RIG EXTENDED Server (Render Stable)
# Unified multi-phase pipeline (RCM + MSCE + RIG + NCR + DEM + QSA)
# ───────────────────────────────────────────────

# Core packaging
setuptools==70.0.0
wheel==0.43.0
pip==25.1.1

# ─── Core Framework ───
fastapi==0.95.2
uvicorn==0.22.0

# ─── Async + Requests ───
httpx==0.26.0
requests==2.31.0
aiofiles==23.2.1   # for async file handling (bias/log/metrics persistence)

# ─── Telegram Bot Bridge ───
python-telegram-bot==20.8

# ─── Data Science Stack ───
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4
scikit-learn==1.3.2  # used for regime modeling + QFT validation
python-dateutil==2.8.2
pytz==2023.3

# ─── Visualization / Dashboards ───
plotly==5.18.0        # HTF/MTF/LTF charts
matplotlib==3.8.2     # auxiliary debugging plots
apscheduler==3.10.4   # background scheduler for auto-refresh

# ─── Environment + Reinforcement ───
gymnasium==0.29.1
stable-baselines3==2.3.2   # optional RL testing integration

# ─── Crypto API Connectors ───
pycoingecko==3.1.0
python-binance==1.0.19
ccxt==4.5.40              # unified Bybit/OKX REST API adapter (upgraded from 4.2.97 — fixes Phemex 30000 error)

# ─── Utilities ───
loguru==0.7.2             # advanced logging (for /logs pipeline)
cachetools==5.3.2         # bias-memory cache support
colorama==0.4.6           # console color output


# ─── Optional Diagnostics ───
psutil==5.9.8             # resource tracking
rich==13.6.0              # improved CLI print for RIG diagnostics

# ─── Testing & Coverage ───
pytest==7.4.3             # testing framework
pytest-asyncio==0.21.1    # async test support
pytest-cov==4.1.0         # coverage reporting
pytest-mock==3.12.0       # mocking utilities


# ───────────────────────────────────────────────
# END OF REQUIREMENTS
# ───────────────────────────────────────────────
